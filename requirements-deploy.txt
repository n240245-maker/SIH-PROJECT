# Runtime-only dependencies for the hosted FastAPI service.
# The deployed API consumes frozen artifacts and does not train or rebuild models.
fastapi==0.141.1
httpx==0.28.1
numpy==2.5.2
orjson==3.12.0
pandas==2.3.3
pydantic-settings==2.15.0
reportlab==4.5.1
uvicorn[standard]==0.52.4
